name: "Build Sphinx docs"
on:
  push:
    branches:
      - main

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: ammaraskar/sphinx-action@master
      with:
        pre-build-command: "cat Makefile; ls -l"
        build-command: "sphinx-build -b html . _build"
        docs-folder: "."
